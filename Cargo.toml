[package]
name = "aoc2023"
version = "0.1.0"
edition = "2021"
authors = ["tobi1805"]

[profile.release]
panic = "abort"

[dev-dependencies]
criterion = { version = "0.5", features = ["html_reports"] }

[[bench]]
name = "bench"
harness = false
